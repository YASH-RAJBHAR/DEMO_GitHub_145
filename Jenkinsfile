pipeline {
    agent any

    stages {
        stage('Start') {
            steps {
                echo "Pipeline started from GitHub"
            }
        }

        stage('Build') {
            steps {
                echo "Building project..."
            }
        }

        stage('Test') {
            steps {
                echo "Testing..."
            }
        }
    }
}
